on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:

  deploy_plan:
    runs-on: ubuntu-24.04
    environment: ${{ inputs.environment }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/reusable/actions/job/plan_iac
